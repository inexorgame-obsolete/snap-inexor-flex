sudo: required
language: python
python:
  - "3.6"

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y snapd snap-confine

install:
  - pip install snapcraft

script:
  - snapcraft
  - ls
